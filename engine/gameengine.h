namespace Engine
{
    class GameEngine
    {
        public:
            GameEngine();
            int initialize();
            int makeLayout();
    };
}
// Initializes the window layout
// static for now
